name: "Custom CodeQL Config"
queries:
  - uses: security-and-quality
query-filters:
  - query: "*"
    exclude:
      when:
        severity: "low"
  - query: "*"
    exclude:
      when:
        severity: "warning"