name: "Custom CodeQL Config"
queries:
  - "security-and-quality"
query-filters:
  - query: "*"
    exclude:
      when:
        severity: "low"
  - query: "*"
    exclude:
      when:
        severity: "warning"