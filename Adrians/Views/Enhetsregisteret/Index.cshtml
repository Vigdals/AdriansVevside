@model List<Adrians.ViewModels.OrgViewModel>

@using (Html.BeginForm("Index", "Enhetsregisteret", FormMethod.Post))
{
    <input type="text" name="orgNummer" required="required" />
    <input type="submit" value="orgNummer" />
}